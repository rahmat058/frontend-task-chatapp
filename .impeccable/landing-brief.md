# Surface: ChatApp landing (`/`)

Mode: Persuade.

Audience: guests and evaluators. Job: believe ChatApp is a realtime messenger and sign in.

Action: Try it now → `/login`. If authenticated, Open app → `/chat` in the nav. `/` always shows this landing.

Proof: authored interactive thread (synthetic; labeled). No testimonials, logos, or metrics.

Direction: Stacked Switchboard (seed `af05fafb`). Night Switchboard identity. Comp: `.impeccable/mocks/decision/assigned.png` (approved). Comp-led.

Memorable moment: the demo thread types indigo/grey bubbles under the offer.

Open: whether +880 remains the only login locale (login already locks it).

---
version: 1
slug: "src-app-page-tsx"
primary_target: "src/app/page.tsx"
related_targets: ["src/app/layout.tsx", "src/components/landing/LandingPage.tsx"]
---

# Surface: ChatApp landing (`/`)

Mode: Persuade.

Audience: guests and evaluators. Job: believe ChatApp is a realtime messenger and sign in.

Action: Open ChatApp → `/login`. If authenticated, the same CTA goes to `/chat`. `/` always shows this landing.

Proof: supplied product preview (`/hero-illustration.png`), labeled as an authored mock. No testimonials, logos, or metrics. No end-to-end encryption claims.

Direction: Graphite Emerald, pinned by `design-style.md` and supplied comps. Split hero 42/58. Compact nav. Three capability columns. Closing CTA.

Memorable moment: the framed product preview next to “Conversation, without the noise.”

Open: login and chat shell still use aliased tokens; they are next in the page-by-page restyle.
